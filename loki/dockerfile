ARG VERSION=3.7.2

FROM grafana/loki:${VERSION}

COPY loki.yml /etc/loki/loki-config.yaml

# Railway volumes mount as root:root; run as root so Loki can write to /loki
USER root

CMD ["-config.file=/etc/loki/loki-config.yaml"]
